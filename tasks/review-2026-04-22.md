# Wave 5 Deep Audit — Consolidated Report (2026-04-22)

10 specialist agents (security, backend, frontend, quality, performance, grep, data, devops, architect, test) ran a parallel read-only deep audit on the post-Wave-4 codebase.

**Total findings (pre-dedup):** 15 Critical + 76 High + 75 Medium + 61 Low = **227**

---

## CRITICAL (15 → 11 after dedup)

| # | ID | File | Issue | Owner |
|---|----|------|-------|-------|
| 1 | C-COOKIE-CSS | `shared/cookie-banner.css` + 35x HTML | Wave 4 extracted CSS but NO `<link>` tag added to any page → every visitor sees unstyled GDPR banner | frontend |
| 2 | C-API-CONTRACT | `shared/js/api-client.js` ↔ `proxy/main.py` | Client POSTs `repair_cost_huf`, `fuel_type`, `symptoms`; Pydantic `extra: "forbid"` → every progressive-enhancement call returns 422 in prod | backend |
| 3 | C-DTC-REGEX | proxy / nginx / JS / DB / build | DTC regex divergent in 5 places (proxy/nginx allow hex `[0-9A-F]{4}`, JS/DB only `\d{4}`) | architect |
| 4 | C-NO-BRANCH-PROT | `main` branch | No branch protection — force-push bypasses every CI check | devops |
| 5 | C-NO-CODEQL | repo | No CodeQL/SAST on `proxy/main.py` (970 lines FastAPI) or `scripts/*.py`; Trivy = image only | devops |
| 6 | C-HTML-CONTINUE | `.github/workflows/ci.yml:151-161` | `html-validate` `continue-on-error: true` masks empty `<h3>`, raw `&`, missing `alt`, long titles | quality |
| 7 | C-CIDR-NO-TEST | `proxy/main.py:92-107` | Wave-4 CIDR-trust matcher (incl. `::ffff:127.0.0.1`, `172.17.0.0/16`) zero tests | quality+test |
| 8 | C-RID-NO-TEST | `proxy/main.py:345-365` | Wave-4 `_sanitize_request_id` (CWE-117) zero tests; loosening regex re-opens log injection | quality+test |
| 9 | C-CAP-NO-TEST | `proxy/main.py:451-482, 700-742` | Wave-4 body cap streamer + negative cache zero tests | quality+test |
| 10 | C-TIMEOUT-NO-TEST | `proxy/tests/test_main.py` MISSING | No respx test for `httpx.TimeoutException` / 429 / 503 / 504 paths | test |
| 11 | C-COOKIE-SKIP | `tests/unit/cookie-consent-types.test.js` | 4 `describe.skip` blocks production-ready today — GDPR/ePrivacy compliance regression guards inactive | quality+test |

---

## HIGH (76 → 46 deduped)

### Security and privacy
- **H-CONFIRM-CSRF**: `hu/confirm.html:137-147` + EN — auto-POST welcome webhook on page load → link-preview bot CSRF (same vector fixed for unsubscribe.html in Wave 4)
- **H-COOKIE-DEADLOCK**: `cookie-consent.js` + `shared/cookie-banner.css` reduced-motion → `transitionend` never fires → `body.overflow:hidden` permanent
- **H-GPC-BYPASS**: `cookie-consent.js:200-226` — stored consent short-circuits GPC checks, breaks EDPB Guidelines 05/2020
- **H-LS-SAFARI**: `cookie-consent.js:200-226` — `window.localStorage` property access throws `SecurityError` in Safari Lockdown / private mode (only `getItem` is in try/catch)
- **H-CONFIRM-MATHRANDOM**: token gen falls back to `Math.random()` → predictable confirm tokens (subscription forgery)

### Backend logic
- **H-ECB-NS**: `scripts/fetch-mnb-rate.py:139` — namespace typo `euref` (should be `eurofxref`); ECB fallback ALWAYS returns "not found" → silent 388.94 hardcoded fallback
- **H-MNB-CRASH**: `scripts/fetch-mnb-rate.py:108` — `rate_elem.text.strip()` crashes on self-closing `<Rate />` → silent fallback chain
- **H-HTTPX-TIMEOUT**: `proxy/main.py:177` — httpx `read=15.0` is per-chunk → slow-loris DoS (8KB every 14s = 2.5h hold per request)
- **H-DTC-CACHE-BYPASS**: `proxy/main.py:850` — `dtc_detail` no `_GET_PARAM_ALLOWLISTS` entry → `?x=1`/`?x=2` bypass negative cache
- **H-LIFESPAN-RACE**: `proxy/main.py:172-191` — `aclose()` then `_http_client = None` races with in-flight `async with .stream()`
- **H-ATOMIC-WRITE**: `build-dtc-database.py:265`, `aggregate-prices.py:482` — non-atomic write; SIGTERM mid-write destroys baseline

### Performance and DoS
- **H-DTC-FAN-OUT**: workshop-finder loops `forEach(searchDTC)` → 50 user codes = 50 `/proxy/dtc/{code}` calls = 30+ visible 429s
- **H-CACHE-STAMPEDE**: `_negative_cache`/`_cache` no `asyncio.Lock` (CSRF has it) → expiry under load = backend stampede
- **H-DTC-DB-SIZE**: `shared/data/dtc-database.json` 915KB monolith + brotli OFF + main-thread JSON.parse → 60-100ms INP breach
- **H-BROTLI-OFF**: `nginx.conf:144-158` brotli commented (alpine base lacks `ngx_brotli`) → +75KB on first load
- **H-FIFO-BOTNET**: `_rate_limits` no `/24` subnet bucket → 10K-IP slow-flow botnet evicts legit users

### Frontend and SEO
- **H-JSONLD-IMG-x10**: 10 blog Schema.org `Article` entries reference non-existent `/images/dtc-lookup-hero.jpg`, `/images/p0420-hero.jpg`, etc → SEO 404s
- **H-XDEFAULT-CONFLICT**: HU + EN homepages disagree on `hreflang="x-default"` → Search Console cluster fail
- **H-EN-BLOG-HREFLANG**: `en/blog/index.html:12` no self `hreflang="en"`, no `x-default` → asymmetric cluster
- **H-TOOL-HUB-NO-HREFLANG**: `hu/eszkozok/index.html` + `en/tools/index.html` ZERO hreflang tags
- **H-SZERVIZ-HREFLANG**: HU `szerviz-kereso.html` missing EN sister hreflang
- **H-ALERT-HU-EXISTS**: 2 EN blog articles `alert("Hungarian version coming soon")` for HU translations that EXIST
- **H-NO-LANG-SWITCH**: `how-ai-car-diagnostics-works.html`, `p0171-dtc-code-causes-symptoms-cost.html` no HU button at all
- **H-DOM-SINK-XSS**: tool pages assign template strings with backend values to `.innerHTML` → defense-in-depth violation
- **H-EVENT-GLOBAL**: `en/blog/index.html:535` `event.target` bare global → undefined in strict-mode contexts
- **H-SZERVIZ-ASCII**: `szerviz-kereso.html:641` ASCII-stripped Hungarian (`Kereses` instead of `Keresés`)

### Devops
- **H-DOCKERFILE-TAG**: `nginx:1.27-alpine3.19` not digest-pinned → mutable surface (tj-actions class supply chain)
- **H-DOCKERFILE-1-STAGE**: `py3-pip` ships in runtime → CVE surface + image bloat
- **H-CONTINUE-ON-ERROR-x3**: smoke tests, html-validate, second Trivy step all `continue-on-error: true` → all "informational"
- **H-BACKEND-URL**: `proxy/main.py:49` defaults to prod URL with no fail-fast → staging-points-to-prod foot-gun
- **H-RAILWAY-NO-MULTI**: `railway.toml` no region/replicas/healthcheckInterval → silent outage

### Test and quality
- **H-API-CLIENT-COVERAGE**: `shared/js/api-client.js` (86 lines) ZERO test imports despite vitest coverage scope
- **H-WEBHOOK-NO-TEST**: newsletter `.catch(() => {})` zero E2E test; user claim "worked" is hearsay
- **H-COOKIE-COVERAGE-GREP**: `cookie-banner-coverage.test.js` regex-greps for `<script src=...>` — never asserts `window.CookieConsent` actually executes
- **H-PARITY-35**: `hu-en-parity.test.js` TOLERANCE 0.35 vs real drift max 3.5% → 10x slack hides logic divergence
- **H-OG-SKIP**, **H-SEOBC-SKIP**, **H-SKIPLINK-SKIP**, **H-COLOR-SKIP**, **H-CACHEBUST-SKIP**: 5 production-ready unskips waiting

### Data integrity
- **H-ESTIMATED-UI**: `vehicle-data.js:213-243` shows `_estimated:true` donor-model prices as authoritative (Suzuki SX4 → Vitara prices) — frontend ignores aggregator's contract
- **H-SCRAPE-TIMESTAMP**: 4 scrapers bake `scrape_timestamp` + `elapsed_seconds` into output → diff churn, blocks reproducibility
- **H-OOYYO-RACE**: `_HASH_CACHE_PATH` write race in CI matrix → discovered hashes lost

### Architecture
- **H-TOOL-DUP**: 6 tool pages × ~400 LOC inline JS duplicated byte-for-byte HU/EN; `hu-en-parity` test guards drift instead of fixing it
- **H-MAIN-PY-GOD**: `proxy/main.py` 886 lines × 6 concerns — God-module
- **H-NO-CONTRACT**: 3 hand-typed copies of calculator request shape (api-client, Pydantic, smoke) — no shared schema

---

## MEDIUM (75 deduped, ~30 unique)

Deferred to per-agent reports for full detail; key items reflected in fix dispatch.

---

## LOW (61 deduped) — DEFER

- 16 placeholder `+36 1 XXX-XXXX` legal pages
- 4 `console.error` in production HTML (newsletter handler)
- 8 `/design/v*.html` orphan mockups (still contain `autocognitix.com`)
- 4 product TODOs in `hu/index.html` (signup, Stripe Pro/Business, case study)
- domain-drift in `seo/*.md` historical docs (5 files)
- minor a11y polish (focus-visible, aria-live regions on tool results)

---

## Wave 5 fix dispatch plan

**Round 1 — Critical (parallel, file-ownership-locked):**
1. **frontend** — cookie-banner.css `<link>` site-wide + cookie-consent.js (reduced-motion, Safari LS, GPC, getSync)
2. **backend** — api-client contract (Pydantic Optional fields), confirm.html click-required, ECB namespace, MNB self-closing, atomic writes
3. **devops** — branch protection, CodeQL default setup, html-validate baseline + drop continue-on-error
4. **test-strategist** — unskip cookie-consent-types + add CIDR/RID/CAP/timeout tests

**Round 2 — High after Round 1 lands:**
5. **backend** — httpx total timeout, DTC negative cache allowlist, asyncio.Lock for cache, CSRF prefetch streaming
6. **frontend** — hreflang fixes (homepages, blog index, tool hubs), alert-to-navigation, DOM-sink hardening
7. **grep-auditor** — fix 10 broken JSON-LD blog images
8. **data-engineer** — `_estimated` UI badge, scraper timestamp removal
9. **performance-analyst** — DTC DB shard, brotli enable (image base swap), DTC fan-out batch endpoint

**Round 3 — Wrap-up:**
10. cleanup: delete `/design/`, fix placeholder phones, drop continue-on-error after fixes, cache-bust unify
