httpx>=0.27.2,<0.28
uvicorn>=0.30.0,<0.46
# Wave 5: bumped from >=0.40.0,<0.42 — CVE-2025-62727 (Starlette DoS via Range
# header merging) is HIGH-severity and Trivy now blocks on HIGH.
# 2026-07-17: bumped 0.49.x -> 1.1.x — CVE-2026-48818 (SSRF/NTLM credential
# theft via UNC paths in StaticFiles, 2x HIGH) is only fixed in 1.1.0.
# Same day: 1.1.x itself is flagged for CVE-2026-54283 (request.form() limits
# silently ignored, HIGH; fixed in 1.3.1) -> pinned to >=1.3.1.
# Verified: all 27 proxy tests pass on starlette 1.3.1 with httpx 0.27.2.
starlette>=1.3.1,<1.4
pydantic>=2.13.4,<3
cachetools>=5.3,<8
